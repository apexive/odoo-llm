<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Thread Form View -->
    <record id="llm_thread_view_form" model="ir.ui.view">
        <field name="name">llm.thread.view.form</field>
        <field name="model">llm.thread</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="active" widget="boolean_toggle" />
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1" />
                        </h1>
                    </div>
                    <group>
                        <group string="Configuration">
                            <field name="user_id" readonly="1" />
                            <field name="provider_id" required="1" />
                            <field name="model_id" required="1" />
                        </group>
                        <group string="Reference">
                            <field name="res_model" readonly="1" />
                            <field name="res_id" readonly="1" />
                            <button
                string="View Record"
                name="action_view_record"
                type="object"
                class="oe_link"
              />
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Thread Tree View -->
    <record id="llm_thread_view_tree" model="ir.ui.view">
        <field name="name">llm.thread.view.tree</field>
        <field name="model">llm.thread</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" />
                <field name="user_id" />
                <field name="provider_id" />
                <field name="model_id" />
                <field name="res_model" />
                <field name="write_date" />
                <button
          name="action_view_record"
          type="object"
          string="View Record"
          class="btn btn-link"
          icon="fa-external-link"
        />
            </tree>
        </field>
    </record>

    <!-- Thread Search View -->
    <record id="llm_thread_view_search" model="ir.ui.view">
        <field name="name">llm.thread.view.search</field>
        <field name="model">llm.thread</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" />
                <field name="user_id" />
                <field name="provider_id" />
                <field name="model_id" />
                <field name="res_model" />
                <separator />
                <filter
          string="My LLM Threads"
          name="my_threads"
          domain="[('user_id', '=', uid)]"
        />
                <filter
          string="Archived"
          name="inactive"
          domain="[('active', '=', False)]"
        />
                <group expand="0" string="Group By">
                    <filter
            string="User"
            name="group_by_user"
            context="{'group_by': 'user_id'}"
          />
                    <filter
            string="Provider"
            name="group_by_provider"
            context="{'group_by': 'provider_id'}"
          />
                    <filter
            string="Model"
            name="group_by_model"
            context="{'group_by': 'model_id'}"
          />
                    <filter
            string="Record Type"
            name="group_by_res_model"
            context="{'group_by': 'res_model'}"
          />
                </group>
            </search>
        </field>
    </record>

    <!-- Actions -->
    <record id="llm_thread_action" model="ir.actions.act_window">
        <field name="name">LLM Thread</field>
        <field name="res_model">llm.thread</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_my_threads': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No LLM threads yet
            </p>
            <p>
                LLM threads will be automatically created when you start
                using LLM responses in your conversations.
            </p>
        </field>
    </record>

    <!-- Menu Items -->
    <menuitem
    id="menu_llm_thread"
    name="LLM Threads"
    action="llm_thread_action"
    parent="llm.menu_llm_root"
    sequence="20"
  />
</odoo>

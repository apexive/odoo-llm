<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Client Action -->
    <record id="action_counter_page" model="ir.actions.client">
        <field name="name">Counter</field>
        <field name="tag">owl_counter.action_counter_page</field>
    </record>

    <!-- Menu Items -->
    <menuitem 
        id="menu_counter_root"
        name="Owl Counter"
        sequence="10"/>
        
    <menuitem
        id="menu_counter"
        name="Counter"
        parent="menu_counter_root"
        action="action_counter_page"
        sequence="10"/>
</odoo>